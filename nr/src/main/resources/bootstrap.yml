server:
  port: 20001
  servlet:
    context-path: /xdnphb/nr/app/matrix
  tomcat:
    uri-encoding: UTF-8
    threads:
      max: 1000  #最大并发数
      min-spare: 20 #初始化时创建的线程数
    max-connections: 20000   #接受和处理的最大连接数
    acceptCount: 700

spring:
  profiles:
    active: dev
  application:
    name: hhhhh

my:
  config:
    fileType: bootstrap
